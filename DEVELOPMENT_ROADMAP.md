# Cheer Up Clinic 발전 기록

## 2026-09-12 앱 입력 8개 기준 XGBoost 재학습

### 완료

- 앱이 실제로 수집하는 입력은 8개인데 기존 XGBoost가 정신건강 4개를 포함한 12개 변수로 학습된 불일치를 확인했다.
- 기존 모델을 앱 방식으로 평가했을 때 Test 가중 재현율이 0.672로 낮아지는 것을 확인했다.
- 인구학 4개, 스마트폰 사용시간 2개, 스마트폰 과의존, 수면의 질로 구성된 8개 앱 입력으로 사전 지정 XGBoost를 재학습했다.
- 모델·하이퍼파라미터·seed 42·60/20/20 분할·Validation 가중 재현율 0.85 이상의 최고 임계값 규칙은 기존과 같게 유지했다.

### 검증 결과

- Train: AUC `0.645665`, Accuracy `0.566898`, Precision `0.543467`, Recall `0.874105`, F1 `0.670226`
- Validation: AUC `0.613267`, Accuracy `0.553361`, Precision `0.535568`, Recall `0.850031`, F1 `0.657116`
- Test: AUC `0.622359`, Accuracy `0.556156`, Precision `0.536482`, Recall `0.859928`, F1 `0.660746`
- Validation 임계값: `0.399846`
- Train–Test AUC 차이: `0.023306`
- 실행환경: Python 3.11.16, scikit-learn 1.5.0, XGBoost 3.2.0
- `train.py`는 한 번만 실행해 Test를 평가했고, 자동 테스트 3개와 Streamlit 기본 설문 제출을 오류 없이 통과했다.

### 제한점 및 다음 우선순위

- 앱에서 수집하지 않는 정신건강 변수를 제외하면서 Validation AUC가 기존 0.641에서 0.613으로 약 0.027 낮아졌다.
- 외부 데이터 검증이 없으므로 개인의 질병 진단이나 발생 확률로 해석할 수 없다.
- 복합표본 위계적 로지스틱 회귀는 연관성 해석용 통계분석으로 별도 유지하고, 8개 입력 XGBoost는 앱 선별용 모델로 구분한다.

## 2026-09-12 복합표본 위계적 로지스틱 회귀 재현

### 완료

- KYRBS 2020 원자료의 `W`(가중치), `STRATA`(층화), `CLUSTER`(PSU)를 모두 사용한 복합표본 분석을 별도 재현 코드로 구성했다.
- 통제변수 → 정신건강 → 스마트폰 → 수면 순서의 위계적 로지스틱 회귀 4개 모형을 Taylor 선형화 방식으로 실행했다.
- 재현 코드와 전체 결과는 Git 저장소 밖의 로컬 `EDA/complex_survey_analysis.py`, `EDA/complex_survey_results/`에 보관했다.

### 검증 결과

- 분석 표본 52,627명, 가중 모집단 2,521,215명, 110개 층, 793개 PSU, 설계 자유도 683을 확인했다.
- 모든 층에 PSU가 최소 2개 존재했으며 단일 PSU 층은 없었다.
- 최종 모형에서 스마트폰 과의존 위험군은 OR 1.789 (95% CI 1.703–1.879, p < 0.001), 수면 문제는 OR 1.252 (95% CI 1.200–1.307, p < 0.001)였다.
- 주중 스마트폰 사용시간은 유의하지 않았고(OR 1.024, p = 0.084), 주말 사용시간은 한 단계 증가할 때 OR 1.034 (p = 0.010)였다.

### 제한점 및 다음 우선순위

- 공개 원자료에 별도 유한모집단보정(FPC) 변수가 없어 FPC는 적용하지 않았다.
- 기존 포트폴리오의 OR 1.82와 1.27은 설계 기반 재현값 1.79와 1.25로 교체할지 검토해야 한다.
- 복합표본 회귀 실행에는 별도 FPC가 포함되지 않았고, Rao–Scott 교차검정은 아직 재현 코드가 없다.

## 2026-08-26 포트폴리오 폴더 구조 고정

### 완료

- 중복된 제출용 폴더 안의 실제 프로젝트를 `oral-health-screening/` 루트로 이동했다.
- 시연 영상, 기획서와 발표자료는 코드 저장소 밖에 유지하고 Python 캐시를 제거했다.
- 화면 렌더링·설문 제출과 데이터·모델 서비스를 검증하는 `tests/`를 추가했다.

### 검증 결과

- 새 프로젝트 경로에서 운영 모듈 문법 검사와 자동 테스트 3개를 통과했다.
- Windows 응용프로그램 제어에 차단된 PyArrow 25 대신 검증된 `pyarrow==21.0.0`을 런타임에 고정했다.

### 제한점 및 다음 우선순위

- GitHub 저장소를 새 프로젝트 루트에 생성하고 공개 파일 목록을 마지막으로 확인해야 한다.

## 2026-08-26 Streamlit 모듈화

### 완료

- 655줄 단일 `app.py`를 52줄 조립 전용 진입점으로 축소했다.
- 설정, 데이터, 모델 처리와 예측·관리·대시보드·정보·레이아웃 화면을 책임별 모듈로 분리했다.
- 실제 예약 저장처럼 보이던 UI를 제거하고 외부 지도 검색과 직접 연락 안내로 명확히 했다.

### 검증 결과

- 전체 운영 모듈 문법 검사를 통과했다.
- Streamlit AppTest에서 9개 탭 렌더링 및 예외 0건을 확인했다.
- 기본 설문 제출 후 모델 예측, 세션 결과 저장, 완료 안내를 확인했다.

### 제한점 및 다음 우선순위

- 자동 테스트를 별도 `tests/` 파일로 고정해 회귀 검사를 간편하게 실행할 필요가 있다.

## 2026-08-26 공개 포트폴리오 정리

### 완료

- 원본 SAS, 행 단위 CSV, PDF, 로컬 출력이 포함된 레거시 노트북을 Git 제외 대상으로 고정했다.
- 공개 앱은 실제 참가자 행 대신 고정 시드 합성 데모 데이터로 대시보드를 표시하도록 변경했다.
- 비식별 모델·스케일러·메타데이터만 실행 자산으로 남기고 `SECURITY.md`에 공개 원칙을 기록했다.
- 운영 경로를 프로젝트 루트 기준 `pathlib.Path`로 고정하고 README를 포트폴리오 구조로 정리했다.

### 검증 결과

- 운영 Python 파일 3개의 문법 검사를 통과했다.
- 모델·스케일러·메타데이터 로드와 단일 예측값 범위 검사를 통과했다.
- 고정 런타임 의존성을 설치하고 Streamlit AppTest에서 예외 0건을 확인했다.
- 실제 서버를 기동해 상태 확인 URL의 HTTP 200 응답을 확인했다.
- 공개 후보 텍스트에서 외부 IP, 이메일, 비밀키, 사용자 절대경로가 검출되지 않았다.

### 제한점 및 다음 우선순위

- 합성 대시보드 수치는 연구 결과가 아니며 UI 시연 용도로만 사용한다.

## 2026-08-26 전처리·선별 모델 재현

### 완료

- 노트북에 있던 KYRBS 2020 전처리 규칙을 `preprocess_data.py`로 추출했다.
- 원본 54,948명에서 스마트폰 사용시간 결측을 제거해 비식별 분석 표본 52,627명을 실제 생성했다.
- Test 성능으로 최고 모델을 선택하던 기존 학습 코드는 `analysis_archive/`로 분리했다.
- XGBoost를 사전 지정하고 Train에서 스케일링·학습, Validation에서 임계값 설정, Test에서 마지막 1회 평가하는 `train.py`를 구성했다.
- 앱 표현을 의료 진단·발생 확률에서 `구강 증상 위험군 선별 보조 점수`로 수정하고 새 임계값 키를 연결했다.

### 실제 검증 결과

- Validation: AUC `0.640639`, Accuracy `0.573360`, Precision `0.549299`, Recall `0.850289`, F1 `0.667429`
- Test: AUC `0.644614`, Accuracy `0.572725`, Precision `0.548059`, Recall `0.854838`, F1 `0.667906`
- Validation 임계값: `0.385317`
- 실행환경: Python 3.11.15, scikit-learn 1.5.0, XGBoost 3.2.0

### 제한점 및 다음 우선순위

- AUC가 약 0.645이고 외부 검증이 없으므로 질병 진단이나 개인 발생 확률로 해석할 수 없다.
- 고정 실행환경을 별도 가상환경으로 재현하고 Streamlit 전체 화면을 확인해야 한다.
- 학교·지역 등 조사 설계의 군집 정보를 이용할 수 있다면 단순 무작위 분할보다 군집·연도 외부 검증을 우선 검토해야 한다.
- 원본 SAS 자료는 재배포 조건을 확인하고 Git 추적에서 제외해야 한다.
