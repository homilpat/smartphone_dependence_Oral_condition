# 공개 저장소 보안 원칙

이 저장소는 포트폴리오 공개용입니다.

- 원본 KYRBS SAS, 행 단위 파생 CSV, 조사 지침·논문 PDF는 Git에 포함하지 않습니다.
- 이름, 연락처, 이메일, 참가자 식별자, 외부 IP, API 키와 비밀번호를 저장하지 않습니다.
- 앱의 공개 통계 화면은 실제 참가자 행이 아닌 고정 시드 합성 데모 데이터를 사용합니다.
- `models/`의 배포 파일은 학습 행이 아니라 XGBoost 트리 파라미터와 표준화 통계만 포함합니다.
- 비밀 값이 필요해지면 `.env` 또는 배포 플랫폼의 Secret 기능을 사용하며 `.env`는 Git에서 제외합니다.

민감정보 노출을 발견하면 공개 이슈에 값을 복사하지 말고 저장소 소유자에게 비공개로 알려 주세요.
